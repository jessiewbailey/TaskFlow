# TaskFlow Kubernetes Secrets Template
# 
# SECURITY WARNING: This is a template file with placeholder values.
# 
# For production deployment:
# 1. Copy this file to secrets.yaml
# 2. Replace all placeholder values with actual credentials
# 3. Consider using external secret management tools like:
#    - Kubernetes External Secrets Operator
#    - HashiCorp Vault
#    - AWS Secrets Manager
#    - Azure Key Vault
# 4. Never commit actual secrets to version control
#
# Alternative: Use base64 encoded values in the 'data' section instead of 'stringData'
#
apiVersion: v1
kind: Secret
metadata:
  name: taskflow-env
  namespace: taskflow
type: Opaque
stringData:
  DATABASE_URL: "postgresql+asyncpg://CHANGE_ME_DB_USER:CHANGE_ME_DB_PASS@postgres:5432/CHANGE_ME_DB_NAME"
  POSTGRES_USER: "CHANGE_ME_DB_USER"
  POSTGRES_PASSWORD: "CHANGE_ME_DB_PASS"
  POSTGRES_DB: "taskflow_db"
  SECRET_KEY: "CHANGE_ME_SECRET_KEY_32_CHARS_MIN"
  AI_WORKER_URL: "http://taskflow-ai:8001"
  BACKEND_API_URL: "http://taskflow-api:8000"
  OLLAMA_HOST: "http://ollama:11434"
  REDIS_URL: "redis://redis:6379/0"